[build]
  command = "npm run build"
  publish = "dist"

# Optional: set VITE_BASE_PATH in Netlify UI when deploying under a subpath.

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
